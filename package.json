{
  "name": "frontwave",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "pnpm --filter frontwave-engine build",
    "dev": "pnpm --filter frontwave-engine dev",
    "typecheck": "pnpm --filter frontwave-engine typecheck",
    "dev:app": "pnpm --filter frontwave-app dev",
    "build:app": "pnpm --filter frontwave-engine build && pnpm --filter frontwave-app build"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
