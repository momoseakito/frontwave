import { writable } from 'svelte/store';

export const cameraRef = writable(null);
export const pixiAppRef = writable(null);
export const hitTesterRef = writable(null);
export const driverRef = writable(null);
